version: 2

formats: []

sphinx:
  builder: html
  configuration: docs/source/conf.py

build:
    image: latest

python:
  version: "3.8"
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
  system_packages: true
